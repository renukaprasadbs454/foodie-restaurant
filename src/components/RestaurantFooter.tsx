import React from 'react';

type RestaurantFooterProps = {
  navigation?: any;
};

/**
 * @deprecated Website-style footers are removed from the restaurant mobile app.
 * Secondary links are moved into Profile & Settings.
 */
export function RestaurantFooter(_props: RestaurantFooterProps) {
  return null;
}

